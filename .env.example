# Optional: opt into the live Telegram smoketest (test #09).
# The bot token is auto-read from ~/.openclaw/openclaw.json if you leave it unset,
# so usually you only need to set the destination chat_id.
# OPENCLAW_TELEGRAM_BOT_TOKEN=123456:abcdef
OPENCLAW_TELEGRAM_CHAT_ID=

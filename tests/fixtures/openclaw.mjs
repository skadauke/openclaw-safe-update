// Entry point for smoketest suite: delegates to the mock implementation.
// Tests reference ctx.installDir/openclaw.mjs; this file sits at the root
// of the fixtures install dir so that path resolves correctly.
import './mock-openclaw.mjs';
